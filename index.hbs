{{!< default}}
<div class="px-12 w-3xl">
  {{#foreach posts}}
    {{> "components/card" page=../pagination.page }} {{!-- partials/card.hbs --}}
  {{/foreach}}

  {{!-- If JS isn't enabled, fallback to standard pagination --}}
  <noscript>
    <div>
      {{pagination}}
    </div>
  </noscript>
</div>