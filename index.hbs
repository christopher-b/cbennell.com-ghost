{{!< default}}

<article-list>
  <h1>Articles</h1>

  {{#foreach posts}}
  {{> "components/card" page=../pagination.page }}
  {{/foreach}}
</article-list>

{{!-- If JS isn't enabled, fallback to standard pagination --}}
<noscript>
  {{ pagination }}
</noscript>
