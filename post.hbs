{{!< default}}

{{#post}}
<article class="{{ post_class }}">
  <header>
    <h1>{{ title }}</h1>

    <div class="post-meta">
      <time datetime="{{date format='YYYY-MM-DD'}}">
        {{date format="DD MMM YYYY"}}
      </time>
      <span>&bull;</span>
      {{ reading_time }}

      {{! tags separator=" • "}}
      {{#if tags}}
      <ul class="tags">
        {{#foreach tags}}
        <li>
          <a href="{{ url }}" title="{{ name }}" class="tag tag-{{ id }} {{ slug }}" rel="tag">
            {{ name }}
          </a>
        </li>
        {{/foreach}}
      </ul>
      {{/if}}
    </div>

    {{> "components/feature-image" feature_image=feature_image }}
  </header>

  <div class="content">
    {{ content }}
  </div>

  <footer>
    <nav aria-label="Next / previous posts">
      <span>
        {{#prev_post}}
        {{> "icons/arrow-left"}}
        Previous:
        <a href="{{ url }}" rel="prev">{{ title }}</a>
        {{/prev_post}}
      </span>

      <span>
        {{#next_post}}
        Next:
        <a href="{{ url }}" rel="next">{{ title }}</a>
        {{> "icons/arrow-right"}}
        {{/next_post}}
      </span>
    </nav>

    {{#if comments}}
    <aside class="comments">
      {{ comments }}
    </aside>
    {{/if}}
  </footer>
</article>

{{/post}}
