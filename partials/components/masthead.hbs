<div class="masthead" aria-labelledby="site-title">
  <a href="{{@site.url}}" rel="home">
    {{#if @site.logo}}
    <img src="{{@site.logo}}" alt="Portrait of Christopher Bennell" />
    {{/if}}

    <h1 id="site-title">
      {{@site.title}}
    </h1>
  </a>

  <p>{{@site.description}}</p>
</div>